<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysPointConfigMapper">
    <resultMap type="SysPointConfig" id="SysPointConfigResult">
        <id property="configId" column="config_id" />
        <result property="pointRate" column="point_rate" />
        <result property="status" column="status" />
        <result property="remark" column="remark" />
        <result property="createBy" column="create_by" />
        <result property="createTime" column="create_time" />
        <result property="updateBy" column="update_by" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="selectSysPointConfigVo">
        select config_id, point_rate, status, remark, create_by, create_time, update_by, update_time from sys_point_config
    </sql>

    <select id="selectSysPointConfigById" parameterType="Long" resultMap="SysPointConfigResult">
        <include refid="selectSysPointConfigVo" />
        where config_id = #{configId}
    </select>

    <select id="selectSysPointConfigList" parameterType="SysPointConfig" resultMap="SysPointConfigResult">
        <include refid="selectSysPointConfigVo" />
        <where>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="selectCurrentPointConfig" resultMap="SysPointConfigResult">
        <include refid="selectSysPointConfigVo" />
        where status = '0'
        order by create_time desc
        limit 1
    </select>

    <insert id="insertSysPointConfig" parameterType="SysPointConfig" useGeneratedKeys="true" keyProperty="configId">
        insert into sys_point_config(point_rate, status, remark, create_by, create_time, update_by, update_time)
        values(#{pointRate}, #{status}, #{remark}, #{createBy}, #{createTime}, #{updateBy}, #{updateTime})
    </insert>

    <update id="updateSysPointConfig" parameterType="SysPointConfig">
        update sys_point_config
        <set>
            <if test="pointRate != null">
                point_rate = #{pointRate},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="remark != null and remark != ''">
                remark = #{remark},
            </if>
            update_by = #{updateBy},
            update_time = #{updateTime}
        </set>
        where config_id = #{configId}
    </update>

    <delete id="deleteSysPointConfigById" parameterType="Long">
        delete from sys_point_config where config_id = #{configId}
    </delete>
</mapper>